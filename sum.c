#include <stdio.h>
sum() {

    int number1, number2, sum;

    printf("\nEnter two integers:\n ");
    scanf("\n%d %d", &number1, &number2);

    // calculating sum
    sum = number1 + number2;

    printf("\n%d + %d = %d\n", number1, number2, sum);
   // return 0;
}

